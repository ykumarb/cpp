#include <iostream>

/* Use all namespace here to avoid clutter */
using namespace std;

int main(void) {

	int a;
	/* Ask inpuut and Get the input from the user */
	cout << "Input: " << "\n";
	cin >> a;
	/* Print the output to the console */
	cout << "Value: " << a << "\n";
	return 0;
}
